version: '3.9'
services:
    client:
        build: ./client
        networks:
            - client
    server:
        build: ./server
        networks:
            - server
networks:
    client:
        driver: bridge
    server:
        driver: bridge
