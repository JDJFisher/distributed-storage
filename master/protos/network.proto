syntax = "proto3";
option go_package = "github.com/JDJFisher/primary-backup/master/network";

message Message {
    string body = 1;
}

service Network {
    rpc SayHello(Message) returns (Message) {}
}

